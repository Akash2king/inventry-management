/** Inline label when a record was returned to this stage and needs fixes. */
export function CorrectionBadge() {
  return (
    <span className="correction-badge" title="Returned from the next stage — review feedback">
      Needs fix
    </span>
  );
}
